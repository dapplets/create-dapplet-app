import { css } from "lit";

export const styles = css`
    .dapplet-button {
        height: 35px;
        padding: 0 10px;
        user-select: none;
        margin: 0 8px 12px 0;
        color: #000;
        font-size: 15px;
        cursor: pointer;
    }
    .dapplet-button:hover {
        background-color: rgba(15, 20, 25, 0.1);
    }
`;
